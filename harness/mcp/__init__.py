# -*- coding: utf-8 -*-
"""MCP(模型上下文协议)那一层 —— 我们**既提供也使用**:

  提供(server.py) —— 把自带工具按协议摆出去, 任何支持这个协议的客户端(桌面 AI 客户端之类)
                     填一行配置 `python -m harness.mcp` 就能连上来调。
  使用(client.py) —— 用户在设置页填外部工具服务的地址/命令, 把它们提供的工具接进模型工具面。

协议本体见 protocol.py(零依赖, 只用标准库)。"""
