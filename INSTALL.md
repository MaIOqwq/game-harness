# 安装与使用

一句话：**浏览器里提问 → 它自己去中文游戏社区真爬一遍 → 引原文作答，答案里每个 `[id=N]` 都能点开看到原帖。**

---

## 一、装之前先确认

| 要什么 | 说明 |
|---|---|
| Windows 10/11 | 这套脚本只按 Windows 写的。 |
| Python **3.11** | 去 python.org 下 3.11，装的时候**勾上 "Add python.exe to PATH"**。 |
| 磁盘 ~3 GB | 依赖 + 两个浏览器内核。 |
| 能上外网 | 一是**装的时候**要能上 GitHub（两个爬虫从那儿取），二是**用的时候**要能访问 bilibili 和 NGA。 |
| Google Chrome（可选） | bilibili 那条链写死了走 Chrome 通道。联网装的时候会替你装一个；**离线包里带了官方的安装包**，`setup.bat` 会在你机器上没有 Chrome 时替你装上（要管理员权限，见第三节末）。 |

> 走下面**第二节（Docker）**那条路的话，表里**只有「能上外网」和「磁盘」两行还算数** —— Python、Chrome 全在容器里，不用你自己装。
>
> **两个爬虫不在这个包里**，装的时候才去取（`game-harness-crawler-nga` / `-bili` 两个独立仓库）：
> 本机 Python 那条路由 `setup.bat` 自动取到 `crawlers\` 下；Docker 那条路由镜像构建时自己 clone。两条路都要求能上 GitHub。

---

## 二、装（Docker：一条命令起全套）

前提：机器上装了 **Docker Desktop**（Windows 上它要用 WSL2，安装时会要你重启一次）。

**1. 填 key。** 在 `harness-dist\` 里新建一个文件，名字就叫 **`.env`**，里面一行：

```
DEEPSEEK_API_KEY=你的key
```

这个文件**不会被打进镜像**（`.dockerignore` 挡着），只是起容器时 compose 读一下。

**2. 一条命令**（在这个目录下开终端）：

```
docker compose up -d --build
```

第一次要构建镜像 —— 下 Python、下两个浏览器内核，**几个 G，慢，十几分钟正常**。构建完就秒开，之后再起不用再建。

**3. 打开 `http://127.0.0.1:8780`** ，就是网页。

**关掉**：`docker compose down`。记忆库和登录态存在 Docker 卷里，下次起来还在。

几条实话：

- **构建时要能上 GitHub**（两个爬虫从那儿 clone）。国内直连 github.com 常被断（实测：容器里 443 直接 Connection refused）。
  Dockerfile 里已经做成**先直连、失败自动走镜像**，正常不用管。要换镜像，在 `.env` 里加一行：
  `GITHUB_MIRROR=https://别的镜像/`（置空 = 只用直连）。
- **两个爬虫跟网页后端在同一个容器里**，不是偷懒 —— 它们把 `127.0.0.1` 写死在代码里，网页后端还要跟它们同机才能「按需拉起」。对外只开 8780 一个口。
- **登录态存在卷里**（bilibili 那份在浏览器用户目录），容器重建不会掉。真想连登录态一起清掉，才用 `docker compose down -v`。
- **记忆库一开始是空的**：包里不带上一台机器的数据，首次启动自己建表，你从零攒。
- 走这条路，**第三节到第五节的「装依赖 / 配 config.bat / 双击 start.bat」全都不用做**。扫码登录、度量降级、工具、排障那些说明照样适用。

---

## 三、装（不用 Docker：本机跑 Python）

双击 **`setup.bat`**。它做五件事：

1. 取两个爬虫（它们是两个独立仓库，`setup.bat` 调 `fetch_crawlers.bat` 取到 `crawlers\nga`、`crawlers\bili`）。
   机器上装了 git 就用 `git clone`（快，以后还能 `git pull` 更新），没有 git 就退回下 ZIP 解压；
2. 建一个 venv（`.venv/`）—— 三个服务共用这一个，不是各装一份；
3. 装本体的依赖（`fpdf2` **必须装** —— 导出 PDF 要用，缺了 `/api/pdf` 直接报错；`tiktoken` 可选）；
4. 装两个爬虫的依赖（bili 那份比较重，几分钟正常）；
5. 下浏览器内核（`chromium` + `chrome`，几百 MB）。

装不动的常见原因就一个：网络断在下载途中。重跑一次 setup.bat 即可（已装好的、已取到的都会跳过）。

> 第 1 步取爬虫时**直连 GitHub 常被断**（尤其国内）。`fetch_crawlers.bat` 里已经做成
> **先直连、失败走镜像、再失败下 ZIP**，正常不用管。要换镜像就改它顶上那行 `MIRROR=`。

> `crawlers\` 里要是有登录态（`browser_data\` 或 `.tool_token`），取爬虫那一步会**跳过不碰** —— 清掉就得重扫码，宁可让你自己决定。
> 真要重取，先自己把那个目录删掉再跑一遍。

**离线包（目录里的 `vendor\`）**：如果这份是从**离线包**里解出来的，包里带了个 `vendor\`，那第 1、3、4、5 步都**不用联网**：

- `vendor\crawlers\` —— 两个爬虫（第 1 步直接拷，不去 GitHub）；
- `vendor\wheels\` —— 依赖轮子（第 3、4 步离线装）；
- `vendor\ms-playwright\` —— **chromium** 内核（第 5 步拷到 `%LOCALAPPDATA%\ms-playwright`，playwright 认这个固定落点）；
- `vendor\chrome\` —— **Chrome 官方的独立安装包**（见下）。

**Chrome 为什么要单独带一份安装包**：Windows 上 `playwright install chrome` 干的是「下 Google 官方的安装包、静默装进系统」，
它**不是一份能放进包里的独立浏览器**，搬不走（这是实测过的：那条命令在 Windows 上只会去找系统里的 Chrome）。
所以离线包改成 —— 把**同一份官方安装包**带上，由 `setup.bat` 在你机器上**没有 Chrome 的时候**替你静默装上。

- 装它**要管理员权限**。没提权的话 `setup.bat` 会明确告诉你，右键「以管理员身份运行」再跑一遍就行。
- **机器上已经有 Chrome 就整个跳过** —— Google 只发布最新稳定版那一份安装包，拿旧包去覆盖更新的版本会被拒装。
- **NGA 不受影响** —— 它先找系统 Chrome，找不到就退回用包里那个 chromium，照样跑。

万一没装上也不致命，只是**B站那条腿起不来**（NGA 照常）。之后自己下一个 Chrome 装上，
或第一次能上网时补一句，都行：

```
.venv\Scripts\python.exe -m playwright install chrome
```


---

## 四、配（只有一项是必填的）

把 `config.example.bat` **复制成 `config.bat`**，填上：

```bat
set DEEPSEEK_API_KEY=你的key
```

`config.bat` 里有密钥，**别外传、别提交到仓库**。不填也能起服务（网页上能看到状态灯），但问不出答案 —— 会直接告诉你缺 key。

其余都是可选项，`config.example.bat` 里逐条写了注释，包括怎么接情感分析服务、怎么关掉调试落盘。

---

## 五、跑

双击 **`start.bat`**。它只起**一个**进程：网页后端（端口 **8780**），然后自己开浏览器到这个地址。

两个爬虫**不在这里起** —— 它们按需启停：网页后端在真要用到某个爬虫时才把它拉起来，闲下来
（默认 10 分钟没人用）自己关掉，连它那份浏览器一起关。这样平时不占内存（两个浏览器常驻着实测约 1 GB）。

代价是**会话里的第一次爬取会多等十几秒** —— 那个爬虫要现拉一次浏览器。属正常。

服务输出写在 `logs/` 下（爬虫被拉起、被关掉，也都记在这里）：

| 文件 | 是谁 |
|---|---|
| `logs/nga.log` | NGA 爬虫 |
| `logs/bili.log` | bilibili 爬虫 |
| `logs/web.log` | 网页后端 |

**关掉**：双击 `stop.bat`（先关网页后端，它会把自己拉起来的爬虫一起带走；再按端口兜底扫一遍）。

> `start.bat` 会先查一遍端口，发现 8770/8771/8780 已被占就直接退出 —— 8780 说明已经起过一份，
> 8770/8771 说明上一轮的爬虫没收干净。都先跑 `stop.bat`。
>
> 想自己控制（比如排障时让某个爬虫一直开着）：单独双击 `bin\run-nga.bat` / `run-bili.bat`，
> 或者在 `config.bat` 里 `set HARNESS_NO_LAZY=1` 把按需启停整个关掉。

---

## 六、第一次打开会看到什么

**两盏灯平时是灰的「未启动」—— 这是正常的，不是坏了。**

爬虫是按需拉起的（见第五节），没在跑的时候它就是灰的。你点「去修复」、或者问一题，它才会启动，
灯随即变成**真状态**：绿 = 登录态正常，红 = 登录态失效。

灯是后端**真去问**爬虫拿的登录态，不是照着假值画的。爬虫没登录，照样能起、也能爬，只是：

- 样本明显变少；
- bilibili 会撞 412 风控，能拿到的更少。

**两个平台都能扫码修** —— 点灯旁边的「**去修复**」，弹窗里出一张**真二维码**：

| 平台 | 用哪个 App 扫 | 备注 |
|---|---|---|
| **bilibili** | B站 App | 后端每 2 秒自己查一次，扫完灯转绿。**扫完要在手机上点「确认登录」才算完**。 |
| **NGA** | NGA App（首页 → 左上角菜单 → 扫一扫） | 同上，扫完灯转绿。 |

扫码是在爬虫**自己那个常驻浏览器**里做的，扫完当场生效，**不用重启服务**。

> 实在扫不了码也可以手工填 cookie：NGA 的在 `crawlers\nga\config.json` 的 `cookies` 段；bilibili 的在 `crawlers\bili\browser_data\` 目录里。填完点灯里的「重新校验」。

**关于 `login_marker`（重要）**：NGA 登录态的判据是「首页里出现没出现某个字串」。原版把这字串写死成了一个演示账号名，换成你的账号就永远判成「没登录」。所以这里做成了可配 —— **用别的账号，必须把 `login_marker` 改成你的 NGA 用户名**，否则灯永远是红的（但爬取本身不受影响）。

---

## 七、度量：情感分析是「关键词降级」

页面上会明写当前用的哪种：

- **关键词降级**（默认）：情感/反讽是词表启发式，不是模型。**分发包里没带情感分析服务**（那个要额外下模型权重，很重）。
- **模型**：你把 `NLP_TOOL_URL` / `NLP_TOOL_TOKEN` 指向自己的 nlp-tool 才会变成这个。

降级不影响能不能出答案，只是「情感」那条线粗一点。**页面上会如实标出来，不会假装有。**

---

## 八、工具：自带的能关，外部的能加

**自带的**（网页「设置 → 工具」，每件一个开关）：

| 工具 | 干什么 | 默认 |
|---|---|---|
| NGA 爬虫 / B站爬虫 | 真去社区爬样本 | 开 |
| 官号探针 | 抓 B站官方账号的动态和评论 | 开 |
| 历史归档检索 | 只读翻本机已存的样本，不新爬 | 开 |
| 情感打分 | 用本机小模型判情绪 / 反讽 | **关** |

关掉哪件，它就从模型手边**整个消失** —— 不是靠提示词劝它别用，是它根本看不见。

情感打分默认是关的，因为分发包里没带那个打分服务（见第七节）。你按第七节配好地址和令牌，它才会出现。

**两种接法，都是 MCP**（现在支持 MCP 的软件很多，编辑器、别的 Agent 都有）：

### 1）别人用我们的工具 —— 我们本身就是一个 MCP 服务

任何支持 MCP 的客户端填上这条命令就能连：

```
命令：  .venv\Scripts\python.exe  -m  harness.mcp
工作目录：harness-dist\
```

连上之后，上面那几件开着的工具（爬 NGA、爬 B站、官号、翻记忆、情感打分）在对方那里就是它自己的工具。
网页「设置 → 外部工具」那一栏里写着这条命令，可以直接照着抄。

### 2）我们用别人的工具 —— 设置页「外部工具」

填一个别处提供的 MCP 服务，它的工具就会一起出现在模型手边。两种填法：

| 接法 | 填什么 | 例子 |
|---|---|---|
| 本地命令 | 一条命令（**有空格就整条填进去**，页面不认引号规则） | `npx -y @modelcontextprotocol/server-filesystem D:\资料` |
| 网址 | 一个 HTTP 地址 | `http://127.0.0.1:8121/mcp` |

右边「**测试**」是当场去连一次 —— 连上了才会显示它提供哪些工具。改完记得点「保存」。

几条实话：

- 外部工具的名字在模型面前会带 `mcp__` 前缀，跟自带的不会撞。
- 外部服务**连不上、卡住，不会把答题拖垮** —— 只是那几件工具当下用不了。页面刷一次**不会**去连外部服务（一个填错的地址能把你卡住一分钟），只有点「测试」才连。
- 外部工具返回的内容**不是我们爬的社区样本**，引不成 `[id=N]`。这点也写进了给模型的工具说明里。

---

## 九、目录里都是什么

```
harness-dist\
  Dockerfile  .dockerignore  compose.yaml   ← Docker 那条路(见第二节)
  setup.bat  start.bat  stop.bat     ← 本机 Python 那条路的 装 / 起 / 关
  fetch_crawlers.bat                 ← setup.bat 会调它；单独点也行（取那两个爬虫仓库）
  vendor\                            ← **只有离线包才有**：依赖轮子 + chromium 内核 + Chrome 安装包 + 两个爬虫（见第三节末）
  config.example.bat                 ← 复制成 config.bat 填 key（本机跑用）
  .env                               ← 走 Docker 的话填 key 用这个（自己建）
  bin\run-nga.bat  run-bili.bat  run-web.bat   ← 单独起某一个(排障用)
  harness\                           ← 引擎 + 工具 + 记忆库 + 网页后端
    webapp.py  网页后端(8780)
    webview.py 把一题的结果翻成页面认的形状
    tools\     自带工具的唯一清单(引擎 / MCP 服务端 / 设置页都读这一份)
    mcp\       MCP 协议层: server.py 我们对外, client.py 我们连别人
    data\      记忆库(sqlite)与每题流水 —— 首次启动自己建, 一开始是空的
  web\index.html                     ← 页面(不再有离线演示数据)
  crawlers\nga\  crawlers\bili\      ← **装完才有**: 两个爬虫是独立仓库, setup.bat 取到这儿。
                                       按需拉起, 平时不在跑; 走 Docker 的话由镜像构建时 clone,
                                       跟网页后端在同一个容器里
  logs\                              ← 跑起来才有
  docs\                              ← 设计与交接文档
```

记忆是**机制跟着走、数据不带**：`schema.sql` + 那几个记忆模块决定表结构，首次启动自己建一个空库，你从零攒自己的记录。包里没有任何上一台机器的数据行。

---

## 十、要排障时看这里

| 现象 | 原因 / 怎么办 |
|---|---|
| **（Docker）**`docker compose up` 报错说找不到 key | `.env` 没建或没填（见第二节第 1 步）。 |
| **（Docker）**构建到一半失败 | 多半是下载断在半路。重跑 `docker compose up -d --build`，已经下好的层会跳过。 |
| **（Docker）**容器起不来、`docker ps` 里没有 | `docker compose logs --tail 100 harness` 看它最后吐了什么。 |
| 装的时候提示「爬虫没取全」 | 上不了 GitHub。换个网络再来一遍，或手工下那两个仓库的 ZIP，把**里面的文件**放进 `crawlers\nga`（`crawlers\bili`）—— 别把解压出来的那层文件夹整个塞进去。 |
| 双击 start.bat 一闪而过 | 多半是没装过或没建 venv。单独双击 `bin\run-web.bat` 会停住不回显 —— 能看清报错。 |
| 灯是**灰**的（未启动） | 正常。爬虫按需拉起，没在跑就是灰的。点「去修复」或问一题，它会自己起。 |
| 灯是**黄**的（unknown） | 服务还没探到登录态。等 30 秒（页面自己会刷），或直接看那两个 log。 |
| 日志像卡住了、半天不写 | 服务是带 `-u` 起的（不带的话 Python 会攒着不写，看起来像卡死）。如果你自己手敲命令起服务，**记得也加 `-u`**。 |
| bili 起不来、报找不到 Chrome | 跑一下 `.venv\Scripts\python.exe -m playwright install chrome`。 |
| 装的时候说「Chrome 没装上」或提示要管理员权限 | 包里那份 Chrome 官方安装包**要管理员权限**才能装。关掉窗口，右键 `setup.bat` 选「以管理员身份运行」重跑一遍；或者自己下一个 Chrome 装上。不装只是 B站那条腿起不来，NGA 照常。 |
| NGA 起不来、报 `No module named 'confluent_kafka'` / `pymysql` | 不该发生了（那两个 import 已经改成可选）。真遇到就跑一遍 `setup.bat`。 |
| 问一题等很久 | 是真的在爬。页面会显示「正在爬取…」并每 3 秒问一次进度，不会像卡死。**会话里的第一题还要多等十几秒** —— 爬虫要现拉一次浏览器。 |
| 爬虫被拉起后十几秒又没了 | 正常，闲置关停（默认 10 分钟没人用）。要它一直开着就 `set HARNESS_NO_LAZY=1`，或单独跑 `bin\run-*.bat`。 |
| 浏览器控制台偶尔刷连接断开的报错 | 已经吞掉了（只吞连接类异常，别的照打）。真还刷，说明是别的错，看 `logs/web.log`。 |
| 外部工具点「测试」连不上 | 那一行下面会写出原因。本地命令那种，多半是命令没填全（`npx` 那类要先装 node）；网址那种，先确认那个地址自己在浏览器里能通。 |
| 别的软件连我们的 MCP 连不上 | 命令里的 python 用**绝对路径**最稳（`.venv\Scripts\python.exe`），工作目录填 `harness-dist\`。手工验一下：在那个目录下敲 `.venv\Scripts\python.exe -m harness.mcp`，它不该报错，而是干等着你喂 JSON。 |

---

## 十一、已知边界（不是缺陷，是还没做）

- **一次一题**。同一个浏览器会话同时只跑一题，第二题会被拒（返回「本题还在跑」）。
- **会话里的第一题会多等十几秒**：爬虫是现拉起来的（见第五节），要拉一次浏览器。
- **新装没登录时，几乎出不了带引用的答案。** 这不是坏了：两个平台都要登录态才有量，尤其 bilibili 未登录时单次查询很慢（要逐条重试），慢到会被本机判定「这条腿不通」。**所以装完第一件事是点灯旁边的「去修复」扫码登录**（见第六节），别急着问。
- **NGA 的登录态判据依赖 `login_marker`**：那是个字串匹配（首页里出现没出现你的用户名），原版写死成演示账号名，用别的账号必须改（见第六节）。**不改的话灯一直是红的，但爬取本身不受影响。**
- **不排队、不多用户**。单用户单会话够用，先没做。
- **外部工具是一次一件、挨着调**（不并发）。它也不占爬取轮次 —— 但反过来，它拿回来的东西不进样本库，不能当引用。
- **必须真的能连上 bilibili / NGA**。爬不到就如实说「本轮样本作废」，不会编。
